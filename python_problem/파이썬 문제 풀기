1. 배스킨라빈스31 게임 (python_problem_1)

## 게임 설명
- 배스킨라빈스31 게임은 마지막 31을 부른 사람이 지는 게임으로, 아래의 규칙을 따른다.
- 참여자들은 번갈아가며 1부터 31까지의 수를 순서대로 부른다.
- 한번에 1~3개의 수를 부를 수 있다.
- 예를 들어, playerA가 1,2,3을 부르면, playerB는 4 또는 4,5 또는 4,5,6를 부를 수 있다.

2. 학생 정보 처리 프로그램 (python_problem_2)

## 프로그램 설명

- 입력 받은 메뉴 번호마다, 각 기능을 수행하는 함수들을 구현할 것.
- 메뉴 1번 함수 : 
    메인에서 학생 정보를 입력 받아 예외 경우 체크 후, 1번 함수
    에게 인자로 데이터를 넘겨, 1번 함수에서 학생 정보를 저장함. 입력 데이터 예
    외경우는 데이터 개수만 고려. 입력 받는 데이터는 이름과 중간 기말 점수이고
    점수는 양의 정수만 입력된다고 가정함. 또한 같은 이름의 학생은 없음

- 메뉴 2번 함수: 
    저장되어 있는 각 학생 정보 중, 학점이 부여되지 않은 학생의
    학점을 계산해서 각 학생의 데이터에 학점을 추가하는 함수를 구현할 것. 학점
    은 중간과 기말의 평균값으로 90점 이상 A, 80점 이상 B, 70점 이상 C, 나머지
    는 D를 부여함

- 메뉴 3번 함수: 
    3번 함수는 저장된 모든 학생의 학점이 부여된 경우만 호출되
    며, 함수는 단지 학생 정보를 출력만 함g

- 메뉴 4번 함수: 
    메인에서 이름을 입력 받아 존재 여부 체크 후, 존재하면 4번
    함수에게 인자로 학생 이름을 넘겨, 함수에서 학생 정보를 삭제함

- 메뉴 5번은 함수로 구현할 필요 없으며, 프로그램을 종료함3  td738

 - 저장되어 있는 학생 정보 있는 경우만, 각 메뉴(2,3,4번)의 해당 함수를 호출할
것. 데이터가 없는 경우는 메뉴 함수를 호출하지 않고 출력 예시와 같은 메시지
출력할 것

- 출력 예시를 보며 프로그램의 각 경우 처리를 이해할 것

- 각 함수의 매개변수, 반환값은 설명과 출력 예시를 보며 구현할 것